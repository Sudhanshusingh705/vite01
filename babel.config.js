module.exports = {
    presets: [
      // Add your desired presets here, e.g., "@babel/preset-react"
    ],
    plugins: [
      // Add other plugins if needed
      "@babel/plugin-proposal-private-property-in-object"
    ]
  };
  